import { ThemingConfig } from '@chakra-ui/react'

// In Chakra UI v3, colorMode config is different
const config = {
    colorMode: {
        initialColorMode: 'light',
        useSystemColorMode: false
    }
}

export default config
